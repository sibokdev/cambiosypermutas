<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disable background -->
    <item android:color="@color/button_bg_gray"
          android:state_enabled="false"/>

    <!-- Disable background -->
    <item android:color="@color/button_bg_light_green"
          android:state_pressed="true"/>

    <!-- Enabled background -->
    <item android:color="@color/button_bg_dark_green"/>
</selector>